# Armor Infer 代码重构修改文档

> 重构日期：2026-06-13
> 重构原则：**功能完全不变**，仅消除代码异味、修复 Bug、提升可维护性

---

## 一、项目背景

本项目是 RoboMaster 竞赛视觉系统，用于装甲板检测与数字识别。基于 YOLOX ONNX 模型进行目标检测，部分场景使用 MLP 网络进行数字分类。

重构前存在以下主要问题：

- `infer.py`、`infer2.py`、`main2.py` 三个文件中存在大量重复的推理逻辑
- `demo_postprocess_armor` 和 `demo_postprocess_buff` 两个函数逻辑几乎完全相同
- 多处硬编码路径和未定义变量引用
- 命名不符合 PEP 8 规范
- 存在约 100 行注释掉的死代码和无用导入

---

## 二、修复的 Bug

### Bug 1：`main2.py` 中使用全局变量名而非 `self`

**位置：** `main2.py` 原第 226 行

**问题：** `Predictor.detect()` 方法中使用了 `predictor.confthre`（全局变量名），而非 `self.confthre`。当全局作用域中不存在 `predictor` 变量时会抛出 `NameError`。

```python
# 修复前（Bug）
detections = self.visual(outputs[0], img_info, predictor.confthre)

# 修复后
detections = self.visual(outputs[0], img_info, self.confthre)
```

### Bug 2：`mlp_predict.py` 中类名拼写错误

**位置：** `mlp_predict.py` 原第 51 行

**问题：** `__main__` 示例代码中使用了不存在的类名 `n`，应为 `number_cls`（现已重命名为 `NumberClassifier`）。

```python
# 修复前（Bug）
recognizer = n("mlp_2.onnx")

# 修复后
recognizer = NumberClassifier("../model/mlp.onnx")
```

### Bug 3：`infer2.py` 中 `roi` 空值检查顺序错误

**位置：** `infer2.py` 原第 233-238 行

**问题：** `draw_detections()` 方法中先调用了 `self.number.predict(roi)`，之后才检查 `roi is not None`。当 `process_roi()` 返回 `None` 时，`predict(None)` 会抛出 `ValueError`。

```python
# 修复前（Bug）
test = self.number.predict(roi)          # roi 可能为 None
num_id = self.number_id[test['class_index']]
if roi is not None:                       # 检查太晚
    rois.append(roi)

# 修复后
num_id = ""
if roi is not None:                       # 先检查再使用
    test = self.number.predict(roi)
    num_id = self.number_id[test['class_index']]
```

### Bug 4：`main2.py` 导入不存在的 `COCO_CLASSES`

**位置：** `main2.py` 原第 7 行

**问题：** `from utils import COCO_CLASSES` 引用了 `datasets.py` 中不存在的名称。实际定义的是 `COCO_CLASSES_tup`、`COCO_CLASSES_buff` 等。

**修复：** 在 `utils/__init__.py` 中添加映射 `COCO_CLASSES = COCO_CLASSES_tup`，保持向后兼容。

### Bug 5：`infer2.py` 中 `get_color_and_tag` 结果被覆盖

**位置：** `infer2.py` 原第 126-129 行

**问题：** `visual()` 方法中调用了 `get_color_and_tag(cls_id)` 并赋值给 `color` 和 `cls`，但紧接着下一行直接用 `cls = self.cls_name[cls_id]` 覆盖了 `cls`，而 `color` 则完全未使用。这是遗留的死代码。

```python
# 修复前（Bug）
color, cls = self.get_color_and_tag(cls_id)   # 计算了但没用
# cls = self.cls_id[cls]s                      # 被注释且有拼写错误
cls = self.cls_name[cls_id]                    # 直接覆盖

# 修复后
cls_id = int(cls_ids[i])
cls = self.cls_name[cls_id]                    # 直接使用，删除无用调用
```

---

## 三、消除重复代码

### 3.1 新建公共基类 `BaseInfer`

**新文件：** `utils/base_infer.py`

**问题：** `infer.py`、`infer2.py`、`main2.py` 中的 `inference()` 方法逻辑完全相同（约 50 行），`__init__` 初始化逻辑高度相似。

**方案：** 提取 `BaseInfer` 基类，封装以下公共逻辑：

| 方法/属性 | 说明 |
|-----------|------|
| `__init__()` | ONNX Session 加载、阈值设置、预处理器初始化、颜色/类别映射 |
| `inference()` | 完整推理流程：预处理 → ONNX 推理 → 解码 → NMS |
| `_parse_position()` | 从检测结果中解析顶点坐标数组 |
| `get_color_and_tag()` | 组合类别索引解码 |
| `_input_name` / `_output_name` | ONNX 输入输出名缓存 |

**继承关系：**

```
BaseInfer
├── Infer        (原 infer)
├── Infer2       (原 infer2)
└── ZoomPredictor (原 Predictor，main2.py)
```

### 3.2 合并 `demo_postprocess_armor` 和 `demo_postprocess_buff`

**文件：** `utils/utils.py`

**问题：** 两个函数仅在分割 apex 坐标数量上不同（4 点 = 前 8 个值 vs 5 点 = 前 10 个值），其余 30+ 行逻辑完全相同。

**方案：** 合并为通用函数 `demo_postprocess(predictions, img_size, num_apex, p6)`，原函数保留为向后兼容包装：

```python
def demo_postprocess(predictions, img_size, num_apex, p6=False):
    """通用的 YOLOX 后处理函数，支持不同数量的 apex 点。"""
    # ... 共享逻辑 ...
    num_coords = num_apex * 2
    splits = np.split(predictions[..., :num_coords], num_apex, axis=-1)
    adjusted = [(s + grids) * expanded_strides for s in splits]
    adjusted_outputs = np.concatenate([*adjusted, predictions[..., num_coords:]], axis=-1)
    return torch.tensor(adjusted_outputs, dtype=torch.float32)

def demo_postprocess_armor(predictions, img_size, p6=False):
    """向后兼容包装函数。"""
    return demo_postprocess(predictions, img_size, num_apex=4, p6=p6)

def demo_postprocess_buff(predictions, img_size, p6=False):
    """向后兼容包装函数。"""
    return demo_postprocess(predictions, img_size, num_apex=5, p6=p6)
```

---

## 四、命名规范化（PEP 8）

| 原名 | 新名 | 向后兼容别名 | 说明 |
|------|------|-------------|------|
| `class infer` | `class Infer` | `infer = Infer` | 类名使用 CapWords 风格 |
| `class infer2` | `class Infer2` | `infer2 = Infer2` | 同上 |
| `class number_cls` | `class NumberClassifier` | `number_cls = NumberClassifier` | 同上 |
| `class Predictor` | `class ZoomPredictor` | — | 体现 zoom 跟踪功能，与 `Infer` 区分 |

所有旧名称均通过模块级赋值保留为别名，确保 `from utils import infer, infer2, number_cls` 等原有导入方式不受影响。

---

## 五、硬编码路径消除

| 文件 | 原代码 | 修改后 |
|------|--------|--------|
| `infer2.py` | `self.number = number_cls("./model/mlp.onnx")` | 新增参数 `mlp_model_path`，默认值 `"./model/mlp.onnx"` |

修改后可通过构造函数灵活指定 MLP 模型路径：

```python
predictor = Infer2(onnx_model_path, num_apex=4, num_class=1, num_color=3,
                   mlp_model_path="./model/mlp_v2.onnx")
```

---

## 六、性能优化

### ONNX 输入输出名缓存

**问题：** `inference()` 方法每次推理都调用 `self.session.get_inputs()[0].name` 和 `self.session.get_outputs()[0].name`，涉及 ONNX Runtime 的内部查询。

**优化：** 在 `__init__` 中缓存到 `self._input_name` 和 `self._output_name`，`inference()` 中直接使用。

```python
# __init__ 中
self._input_name = self.session.get_inputs()[0].name
self._output_name = self.session.get_outputs()[0].name

# inference() 中
outputs = self.session.run([self._output_name], input_feed={self._input_name: img})
```

---

## 七、死代码清理

### 删除的代码

| 文件 | 删除内容 | 行数 |
|------|---------|------|
| `main.py` | 顶部无用导入：`poly_postprocess, vis, min_rect, ValTransform, demo_postprocess_armor, demo_postprocess_buff` | 1 行 |
| `main.py` | 注释掉的旧 `__main__` 块 | ~28 行 |
| `main2.py` | `import os`（未使用） | 1 行 |
| `main2.py` | `import time`（仅注释代码使用） | 1 行 |
| `main2.py` | 注释掉的旧 `__main__` 块 | ~28 行 |
| `utils/utils.py` | 注释掉的旧 `demo_postprocess` 函数 | ~37 行 |
| `infer2.py` | `visual()` 中 `get_color_and_tag` 死代码调用 | 2 行 |

**合计清理约 100 行死代码。**

---

## 八、`infer2.py` 静态分析问题修复（7 项）

以下为 IDE 静态检查工具发现的问题，均已在第二轮中修复：

### 问题 1 & 5 & 6：PEP 8 E203 — 冒号前多余空格

**位置：** 第 43 行、第 146 行

**问题：** Python 切片语法中冒号前不应有空格。

```python
# 修复前
boxes = output[:, 0 : self.num_apexes * 2]
number_image = number_image[y : y + roi_size[1], x : x + roi_size[0]]

# 修复后
boxes = output[:, 0:self.num_apexes * 2]
number_image = number_image[y:y + roi_size[1], x:x + roi_size[0]]
```

### 问题 2：意外类型提示

**位置：** 第 43 行

**问题：** 与 E203 同源，切片语法格式修正后类型推断恢复正常。

### 问题 3：方法应为静态方法

**位置：** 第 73 行 `calculate_pdistance`

**问题：** 该方法未使用 `self` 参数，应声明为 `@staticmethod`。

```python
# 修复前
def calculate_pdistance(self, pt1, pt2):

# 修复后
@staticmethod
def calc_distance(pt1, pt2):
```

### 问题 4：未使用的形参

**位置：** 第 98 行 `process_roi(self, raw_img, pts, num_apexes)`

**问题：** `num_apexes` 参数在方法体内从未使用。调用方传入 `self.num_apexes` 但方法内部不依赖该值。

```python
# 修复前
def process_roi(self, raw_img, pts, num_apexes):
    # ... num_apexes 未被使用 ...
roi = self.process_roi(frame, pts2, self.num_apexes)

# 修复后
def process_roi(self, raw_img, pts):
    # ... 移除无用参数 ...
roi = self.process_roi(frame, pts2)
```

### 问题 7：方法名拼写错误

**位置：** 第 73 行 `calculate_pdistance`

**问题：** `pdistance` 不是合法英文单词，推测为 `point distance` 的缩写残留。重命名为 `calc_distance`，语义更清晰。

```python
# 修复前
def calculate_pdistance(self, pt1, pt2):
# 调用处
length1, center1 = self.calculate_pdistance(pts[0], pts[1])

# 修复后
@staticmethod
def calc_distance(pt1, pt2):
# 调用处
length1, center1 = self.calc_distance(pts[0], pts[1])
```

---

## 九、文件变更清单

| 文件 | 操作 | 主要变更 |
|------|------|---------|
| `utils/base_infer.py` | **新建** | 公共基类 `BaseInfer`，封装推理核心逻辑 |
| `utils/utils.py` | 修改 | 合并 postprocess 函数，清理注释代码 |
| `utils/infer.py` | 修改 | `Infer` 继承 `BaseInfer`，添加别名 `infer` |
| `utils/infer2.py` | 修改 | `Infer2` 继承 `BaseInfer`，修复 roi 空值 bug，消除硬编码路径，修复 7 项静态分析问题 |
| `utils/mlp_predict.py` | 修改 | 重命名为 `NumberClassifier`，修复 `__main__` bug，添加别名 |
| `utils/datasets.py` | 修改 | 添加 `__all__` 导出列表 |
| `utils/__init__.py` | 修改 | 更新导出，添加 `COCO_CLASSES` 映射和 `BaseInfer` 导出 |
| `main.py` | 修改 | 清理无用导入和注释代码 |
| `main2.py` | 修改 | `ZoomPredictor` 继承 `BaseInfer`，修复 `self.confthre` bug，清理死代码 |

---

## 十、向后兼容性

以下原有导入方式均保持可用，无需修改调用方代码：

```python
from utils import infer                    # ✅ 可用（别名）
from utils import infer2                   # ✅ 可用（别名）
from utils import number_cls               # ✅ 可用（别名）
from utils import poly_postprocess         # ✅ 可用
from utils import vis, min_rect            # ✅ 可用
from utils import ValTransform             # ✅ 可用
from utils import demo_postprocess_armor   # ✅ 可用
from utils import demo_postprocess_buff    # ✅ 可用
from utils import COCO_CLASSES             # ✅ 可用（映射到 COCO_CLASSES_tup）
```

新增的推荐导入方式：

```python
from utils import Infer, Infer2, NumberClassifier, BaseInfer
from utils import demo_postprocess         # 通用后处理函数
```

---

## 十一、验证建议

由于本项目涉及 ONNX 模型推理和视频处理，建议在有完整环境的机器上执行以下验证：

1. **导入验证：** 运行 `python -c "from utils import *"` 确认无导入错误
2. **功能验证（main.py）：** 使用 `yolox.onnx` + `Infer2` 模式处理 `./video/1.mp4`，对比重构前后的输出视频
3. **功能验证（main2.py）：** 使用 `buff_300.onnx` + `ZoomPredictor` 处理 `./video/4.avi`，确认 zoom 功能正常
4. **向后兼容验证：** 确认原有代码中 `from utils import infer, infer2` 等导入仍可正常使用
